import styled from '../../node_modules/styled-components';

const S = {
    App: styled.div`
        box-sizing: border-box;
        @media (min-width: 850px) {
            display: flex;
            flex-direction: row;
        } 
        `,

}

export default S;